diff --git a/src/hints.ts b/src/hints.ts
index e444444..e555555 100644
--- a/src/hints.ts
+++ b/src/hints.ts
@@ -2,7 +2,8 @@ export function renderHints(hints: string[]) {
   if (hints.length === 0) return "";
   const cleaned = hints.filter(Boolean);
   const numbered = cleaned.map((hint, idx) => `${idx + 1}. ${hint}`);
-  return numbered.join("\n");
+  const preview = numbered.slice(0, 20);
+  return preview.join("\n");
 }
