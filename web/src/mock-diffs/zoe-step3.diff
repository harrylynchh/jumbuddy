diff --git a/src/schedule.ts b/src/schedule.ts
index d333333..d444444 100644
--- a/src/schedule.ts
+++ b/src/schedule.ts
@@ -1,8 +1,8 @@
 export function buildSchedule(events: Event[]) {
   const out: Event[] = [];
 
   for (const e of events) {
     if (e.enabled) out.push(e);
   }
-
+  out.sort((a, b) => a.start - b.start);
   return out;
 }
